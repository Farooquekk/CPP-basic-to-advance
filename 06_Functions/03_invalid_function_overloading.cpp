#include <iostream>
using namespace std;

// int func(int a);      // Uncommenting both will cause a compilation error
// double func(int a);

int main() {
    // func(5);          // Ambiguous call, 
    return 0;
}
